services:
  db:
    image: postgres:16-alpine
    container_name: secretarai-postgres
    environment:
      POSTGRES_DB: secretarai
      POSTGRES_USER: secretarai_user
      POSTGRES_PASSWORD: secretarai_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

  api:
    build:
      context: ./api
      dockerfile: Dockerfile
      image: sarahstrawberries/secretarai-api:0.0.1
    container_name: secretarai-api
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:5000
      - ConnectionStrings__DefaultConnection=Host=db;Database=secretarai;Username=secretarai_user;Password=secretarai_password
    ports:
      - "5000:5000"
    depends_on:
      - db
    restart: unless-stopped

  web:
    build:
      context: ./web
      dockerfile: Dockerfile
      image: sarahstrawberries/secretarai-web:0.0.1
    container_name: secretarai-web
    environment:
      - VITE_OIDC_AUTHORITY=https://auth-dev.snowse.io/realms/secretarai
      - VITE_OIDC_CLIENT_ID=858424803841-jmb7ase4p06bpanbpphf2thij03hoqm8.apps.googleusercontent.com
      - VITE_OIDC_REDIRECT_URI=https://auth-dev.snowse.io/realms/secretarai/broker/google/endpoint
    ports:
      - "3000:80"
    depends_on:
      - api

volumes:
  postgres_data: